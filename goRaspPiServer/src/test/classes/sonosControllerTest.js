var chai = require('chai');
var expect = chai.expect;
var SonosController = require('./../../../src/main/classes/sonosController');
var s = require('sonos');
var sonosController = new SonosController(s);

describe('SonosController', function() {
  it('getNumberOfSpeakers() should return the number of speakers on your wifi', function() {
    
  });
});
